<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.po.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="passWord"/>
    </resultMap>

    <select id="userByUsername" parameterType="java.lang.String" resultType="com.example.demo.po.User">
        select username,password from user
        <if test="username != null">
            username= ${username}
        </if>
        <if test="password != null">
            password= ${password}
        </if>
    </select>

    <insert id="insert" parameterType="com.example.demo.po.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">

            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
            <if test="userLevelId != null">
                userLevelId,
            </if>
        </trim>
    </insert>


</mapper>
